# Web Agent Notes

Follow the focused rules in [`agents-skills/`](./agents-skills/).

Current rules:
- [`vue-props-reactivity.md`](./agents-skills/vue-props-reactivity.md): avoid destructuring Vue props in component setup when the values must stay reactive.
- [`clean-vue-components.md`](./agents-skills/clean-vue-components.md): keep Vue components focused on rendering and user actions; move option catalogs, normalization logic, and reusable state handling into nearby modules or composables.
