[build]
  command = "pnpm --filter @arc/storefront generate"
  publish = "apps/storefront/dist"
  ignore = "git diff --quiet \"$CACHED_COMMIT_REF\" \"$COMMIT_REF\" -- apps/storefront packages package.json pnpm-lock.yaml pnpm-workspace.yaml"
