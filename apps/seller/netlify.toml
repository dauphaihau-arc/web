[build]
  command = "pnpm --filter @arc/seller generate"
  publish = "apps/seller/.output/public"
  ignore = "git diff --quiet \"$CACHED_COMMIT_REF\" \"$COMMIT_REF\" -- apps/seller packages package.json pnpm-lock.yaml pnpm-workspace.yaml"
