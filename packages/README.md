# Shared Packages

This directory is reserved for cross-app packages.

The current phase only establishes the monorepo structure. Shared code is still inside `apps/storefront/src/shared` and will be extracted later.
